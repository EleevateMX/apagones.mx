{
  "name": "apagonesmid",
  "version": "1.0.0",
  "private": true,
  "description": "ApagonesMID — app cívica de reportes de apagones y fugas en Mérida",
  "scripts": {
    "build": "node copy-web.js",
    "sync": "npm run build && cap sync ios",
    "ios": "npm run sync && cap open ios"
  },
  "dependencies": {
    "@capacitor/app": "^7.0.0",
    "@capacitor/core": "^7.0.0",
    "@capacitor/ios": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
